[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flashml"
version = "0.6.1"
authors = [{ name = "kbradu" }]
dependencies = [
    "ipywidgets",
    "mlflow",
    "matplotlib",
    "plotly",
    "numpy",
    "torch",
    "torchtune",
    "torchao",
    "torchinfo",
    "tqdm",
    "psutil",
    "windows-curses",
    "playsound==1.3.0",
    "pillow",
    "pynvml",
    "scikit-learn",
    "pynput",
    "pyautogui",
    "pyperclip",
    "tkinterdnd2",
    "joblib",
    "polars",
    "pandas",
    "spacy",

    "python-dotenv",
    "ollama",
    "torch-optimi",
    "gymnasium",
    "transformers",
    "peft",
    "ipykernel",
    "opencv-python",
    "sphinx",
    "sphinx-rtd-theme",
    "sphinx-autodoc-typehints",
]

[tool.setuptools]
packages = ["flashml"]
